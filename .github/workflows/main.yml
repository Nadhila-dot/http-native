name: Benchmark

on:
  workflow_dispatch:
    inputs:
      engines:
        description: 'Comma-separated engines to benchmark. Supported by this workflow: "http-native,bun"'
        required: false
        default: "http-native,bun"
      scenarios:
        description: 'Comma-separated scenarios: "static,dynamic,opt"'
        required: false
        default: "static,dynamic,opt"
      connections:
        description: "Bombardier concurrency"
        required: false
        default: "200"
      duration:
        description: "Bombardier duration"
        required: false
        default: "10s"
      timeout:
        description: "Bombardier timeout"
        required: false
        default: "2s"

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: Install bombardier
        env:
          BOMBARDIER_BIN: ${{ runner.temp }}/bombardier
        run: |
          go install -o "$BOMBARDIER_BIN" github.com/codesenberg/bombardier@latest
          echo "BOMBARDIER_BIN=$BOMBARDIER_BIN" >> $GITHUB_ENV
          chmod +x "$BOMBARDIER_BIN"
          "$BOMBARDIER_BIN" -version || (echo "bombardier failed to install" && exit 1)

      - name: Run benchmarks
        run: |
          bun bench/ci.js \
            --engines="${{ github.event.inputs.engines || 'http-native,bun' }}" \
            --scenarios="${{ github.event.inputs.scenarios || 'static,dynamic,opt' }}" \
            --connections="${{ github.event.inputs.connections || '200' }}" \
            --duration="${{ github.event.inputs.duration || '10s' }}" \
            --timeout="${{ github.event.inputs.timeout || '2s' }}"

      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: |
            bench/results/results.json
            bench/results/summary.md
          if-no-files-found: ignore
